apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: basic-app-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - basic-app-gateway.jiangfwa-dev.dev.meetapi.webex.com
